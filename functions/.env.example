# Optional. When unset, lookupProductByBarcode uses UPCitemdb's free trial
# endpoint (~100 lookups/day shared). Set this to your paid API key to use
# the production endpoint with higher rate limits.
UPCITEMDB_API_KEY=
